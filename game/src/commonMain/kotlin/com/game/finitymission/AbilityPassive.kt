package com.game.finitymission

class AbilityPassive(
    state: GameState,
    duration: Int,
    from: Actor,
) : Ability(state, duration, from) {

    override fun tick() {
        super.tick()
    }
}